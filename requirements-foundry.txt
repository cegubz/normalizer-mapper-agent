# Base agent dependencies
# -r requirements.txt

# Foundry Hosted Agent HTTP host (Responses protocol) — OpenAI-compatible
# POST /responses, GET/DELETE /responses/{id}, POST /responses/{id}/cancel.
azure-ai-agentserver-responses==2.1.0

# Foundry model gateway (models from the Foundry catalog) + Entra ID auth.
# Used only when MODEL_PROVIDER=foundry.
azure-identity
azure-ai-projects    
starlette        # provides AIProjectClient.get_openai_client(); pin to the
                             # version that matches your Foundry project if needed.
pandas
openpyxl 
openai
azure-functions
# Optional (enable when STORAGE_BACKEND=azure_blob) — see core/storage.AzureBlobStorage:
azure-storage-blob
azure-identity  # only needed for the no-SAS/no-connection-string Entra ID auth fallback
azure-ai-projects
httpx